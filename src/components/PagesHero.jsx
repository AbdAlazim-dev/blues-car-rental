function PagesHero({ section, sectionDiscription }) {
  return (
    <section className="pages-hero">
      <h1>{section}</h1>
      <h4>{sectionDiscription}</h4>
    </section>
  );
}
export default PagesHero;
